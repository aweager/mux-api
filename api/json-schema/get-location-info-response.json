{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mux-api.local/schemas/get-location-info-response.json",
  "title": "GetLocationInfo Response",
  "description": "Response for MuxAPI.GetLocationInfo method. If the location doesn't exist, exists is false and id is omitted.",
  "type": "object",
  "required": ["exists"],
  "properties": {
    "exists": {
      "type": "boolean",
      "description": "Whether the location reference exists"
    },
    "id": {
      "type": "string",
      "description": "The canonical ID of the location (only present when exists is true)"
    }
  }
}
